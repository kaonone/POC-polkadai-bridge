{
  "name": "subgraph",
  "license": "MIT License",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ polkadai-bridge/subgraph",
    "create-local": "graph create --node http://localhost:8020/ polkadai-bridge/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ polkadai-bridge/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polkadai-bridge/subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.1",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
